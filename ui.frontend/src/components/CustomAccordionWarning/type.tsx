import { MappedComponentProperties } from "@adobe/aem-react-editable-components";

export interface CustomAccordionWarningProps extends MappedComponentProperties {
    headline: string;
    subheadline: string;
    content: string;
}
